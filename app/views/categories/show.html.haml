%p#notice= notice

%p
  %b Demoty z kategorii:
  = @category.name
%ul
  - @category.demots.each do |demot|
    %li= link_to image_tag(demot.image.url), demot
- if can? :edit, @category
  = link_to 'Edit', edit_category_path(@category)
  \|
= link_to 'Back', categories_path
